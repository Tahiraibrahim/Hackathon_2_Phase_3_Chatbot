/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    tailwindcss: {},   // Note: Hum wapas 'tailwindcss' par aa gaye hain
    autoprefixer: {},
  },
};

export default config;